name: Apply Binance-only summary patch

on:
  push:
    branches:
      - fix/binance-only-top-summary

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/binance-only-top-summary
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply_binance_summary_fix.py
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Use Binance account value for top summary'
          git push
